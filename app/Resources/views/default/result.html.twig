{% extends 'base.html.twig' %}
{% block body %}
<body>
  <div class="zone_affichage container">
    <div class="row">
      <div class="col-lg-12 col-xs-12">
        <div class="panel panel-default">
          <div class="panel-heading">
            <i class="fa fa-users"></i> Résultat de la recherche
          </div>
          <div class="scroll">
              {% for users in users %}
                      <a href="/profil/{{users.id}}"><button class="btn btn-default btn-lg btn-block"><img src="images/Avatar/{{users.id}}.png" class="photos_amis" alt="{{ users.name }} {{ users.lastname}}" id="miniature"></img>
                      {{ users.name }} {{ users.lastname}}</button></a>
              {% endfor %}
          </div>
        </div>
      </div>
    </div>
  </div>

</body>

{% endblock %}
